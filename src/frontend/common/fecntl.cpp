/* frontend/common/fecntl.cpp -- RECONSTRUCTED (TYPE-DEFINITION-ONLY TU; emits no code or data)
 *
 *   Fecntl.obj (SYM FILE-block, nfs4-f-v2.txt lines 259218-260751) is a PURE TYPE-DEFINITION
 *   translation unit: it contributes ZERO .text functions and ZERO .data/.bss symbols to the
 *   linked image. Verified three ways:
 *     (1) SourceLineMap.jsonl  -> 0 rows for FECNTL.CPP (no SLD line records = no code).
 *     (2) Symbols.jsonl tu_obj="Fecntl.obj" -> 0 symbols (no EXT fcn/data definitions).
 *     (3) raw SYM block -> 0 records with a .text VA ($800xxxxx); only debug TYPE metadata:
 *           81 STRTAG + 710 MOS + 125 MOE + 11 ENTAG + 44 TPDEF + 15 FIELD + 1 UNTAG  (112 distinct
 *           type names). These are the PsyQ / libgte / libgpu / libpad standard types this TU's
 *           headers pulled in (PAD_PSX, PAD_NEGCON, PAD_ANALOG, MATRIX, VECTOR, SVECTOR, CVECTOR,
 *           RVECTOR, CRVECTOR3/4, PSX_RECT, DR_ENV, ...). The SYM debug format attributes each type to
 *           the first .obj that references it; Fecntl.obj (early in link order) absorbed them.
 *
 *   All 112 types are ALREADY reconstructed in ../../nfs4_types.h (verified present). There is no
 *   source-level code or data to emit here -- the original fecntl.cpp compiled to type info only
 *   (inline-only / declaration-only content). This stub records the finding for completeness.
 */
#include "fecntl.h"
/* (intentionally empty: no functions, no data) */
/* end of fecntl.cpp */
