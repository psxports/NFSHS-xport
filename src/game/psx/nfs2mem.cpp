/* game/psx/nfs2mem.cpp -- RECONSTRUCTED (TYPE-DEFINITION-ONLY TU; emits no code or data)
 *
 *   nfs2mem.obj (SYM FILE-block, nfs4-f-v2.txt lines 212219-212621) is a PURE TYPE-DEFINITION
 *   translation unit: it contributes ZERO .text functions and ZERO .data/.bss symbols to the
 *   linked image. Verified three ways:
 *     (1) SourceLineMap.jsonl  -> 0 rows for NFS2MEM.CPP (no SLD line records = no code).
 *     (2) TUSummary.jsonl tu_obj="nfs2mem.obj" -> ARG=0 AUTO=0 REG=0 REGPARM=0 EXT=0 STAT=0
 *         (zero functions, zero external fcn/data defs, zero statics). Only debug TYPE metadata:
 *         38 STRTAG + 216 MOS + 4 MOU + 1 UNTAG + 90 TPDEF + 13 FIELD + 39 EOS (total 401 records).
 *     (3) raw SYM block (v2.txt 212219..) opens with `94 Def class FILE ... name nfs2mem.obj` and
 *         holds ONLY type Def/Def2 records ($00000000 VA) -- no 8c/8e function-start records, no
 *         .text VAs.
 *
 *   The 35 struct tags + 90 typedefs are the PsyQ / libgte / libgpu / libcd / libpad / Draw standard
 *   types this TU's headers pulled in (the SYM format attributes each type to the first .obj that
 *   references it; nfs2mem.obj absorbed them):
 *     COORD16, CRVECTOR3, CRVECTOR4, CVECTOR, DIRENTRY, DISPENV, DRAWENV, DR_ENV, DR_MODE,
 *     DRender_tCalcView, DVECTOR, Draw_CarVertex, Draw_PrimStruct, Draw_SVertex, Draw_tCacheHeader,
 *     Draw_tPixMap, EXEC, MATRIX, PAD_ANALOG, PAD_COMMON, PAD_MOUSE, PAD_NEGCON, PAD_PSX, PCOORD16,
 *     PSX_RECT, RVECTOR, SHAPETABLEENTRY, SVECTOR, TCB, VECTOR, cdstreamstruct, kernpair, linedef,
 *     matrixtdef, shapetbl (+ union ._N).
 *
 *   All 35 struct types are ALREADY reconstructed in ../../nfs4_types.h (verified present). There is
 *   no source-level code or data to emit here. This stub records the finding for completeness.
 *
 *   No SYM AUTO/REGPARM locals exist for this TU (0 functions), so the SYM-locals rule is satisfied
 *   vacuously.
 */
#include "nfs2mem.h"
/* (intentionally empty: no functions, no data) */
/* end of nfs2mem.cpp */
